# Full ingest

Work from the supplied raw-note list and synthesis. Extract durable claims and provenance, searching existing wiki pages before creating new ones. Update the existing concept page when possible; create only reusable knowledge. Preserve raw notes, and report any supplied path that does not warrant synthesis.

Open wiki pages with an abstract callout. Cite factual claims back to raw-note blocks, distinguish inference from source claims, and preserve contradictions and retrieval limits. Resolve author instructions against actual vault state; publish settled reader-facing facts, not dispatch wording or promised future page states.

Update the plain-Markdown navigation index for content pages in scope, register useful missing tags, and add meaningful related links. Record date, input paths, changed pages, and verified outcomes in the log. Reconcile existing prose in the paragraphs you touch instead of appending a competing claim. Check the complete changed batch before claiming completion. When fixing a recurring defect, record its scope and files checked; do not generalize a batch check to the whole vault.

Report paths incorporated into wiki separately from paths left un-indexed, plus changes, source limitations, and checks that could not run.
