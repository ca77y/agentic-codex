# Correct code or test findings

Read the routed findings, validated acceptance source, current code, and other owners' paths. Implement a bounded correction; add meaningful missing regression tests when needed. Report a contract mismatch before affected production instead of rewriting requirements. Preserve unrelated edits and do not temporarily revert shared source.

Return addressed locations, unresolved findings, changed tests, and suggested unexecuted commands. The main agent obtains fresh QA; continued production uses the same remaining attempt allowance.
